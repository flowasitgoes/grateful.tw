rules_version = '2';

// 預設全拒。Collection 先在 docs/sdd/05-data-models.md 定欄位，再改本檔。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
