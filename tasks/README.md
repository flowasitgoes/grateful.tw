# tasks/

單次實作依據。

- **只有這個 `README.md` 進 git。**
- 其餘 `tasks/*.md` 本機用，不要 commit。
- 只有改執行邏輯才建 task。改 PRD／SDD／README 免建 task。
- 一個 task 對一個可獨立 review 的 branch／功能。
- 進程式碼前先做 Spec Check：範圍、AC、資料模型、API、UI 狀態、測試都要能對上。缺漏就停、回報。

建議檔名：`YYYYMMDD-短名.md`。
