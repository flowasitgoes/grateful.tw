# 05 — 資料模型

P0 沒有使用者輸入、沒有後端文件。第一篇日記是**寫死在前端的靜態文稿**，與畫面一對一。

| 儲存 | 本產品是否使用 |
| --- | --- |
| 靜態文稿（元件 template） | P0 使用 |
| localStorage | 不使用 |
| Firestore / Auth | 不使用 |

不得加 collection、表單欄位、或把這篇文稿拆成可編輯欄位。
