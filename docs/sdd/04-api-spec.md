# 04 — API 契約（Callable／事件）

**沒有對外 REST `/api`。** P0 無後端呼叫。

## P0

靜態文稿，無 Client SDK、無 Callable、無 Auth。

## Callable

| 名稱 | 依據 |
| --- | --- |
| （無） | P0 不需要 |

## 明確不做

- 對外 HTTP `/api`
- 為這篇日記自建儲存 API
